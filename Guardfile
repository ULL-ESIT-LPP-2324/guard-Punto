# A sample Guardfile
# More info at https://github.com/guard/guard#readme
#
# https://github.com/rubyist/guard-rake

guard 'rake', :task => 'doit', :run_on_all => true do
  watch(%r{lib/punto.rb})
  watch(%r{spec/punto_spec.rb})
end
